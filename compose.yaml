services:
  browser-connector:
    environment:
      - CAMOUFOX_DEBUG=${CAMOUFOX_DEBUG:-false}
      - CAMOUFOX_GEOIP=false
      - CAMOUFOX_MODE=pool
      - CAMOUFOX_POOL_SIZE=3
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    image: ghcr.io/oyedejioyewole/browser-connector:edge
    ports:
      - 127.0.0.1:3000:3000
      - 127.0.0.1:8080:8080
    restart: unless-stopped

  browser-connector-gateway:
    depends_on:
      browser-connector:
        condition: service_healthy
    environment:
      - NITRO_APP_SECRET=${NITRO_APP_SECRET?:Missing NITRO_APP_SECRET}
      - NITRO_UPSTREAM_URL=${NITRO_UPSTREAM_URL?:Missing NITRO_UPSTREAM_URL}
    image: ghcr.io/oyedejioyewole/browser-connector-gateway:latest
    restart: unless-stopped
